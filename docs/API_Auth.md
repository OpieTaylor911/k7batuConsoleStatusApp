When a device is firts setup with the sidekick tool it gets a API Token that is setup on the uconsole. It the sidekick app we can set this to overwrite and restart the api in uconsole, but the API key should be something like "k7_sk_3DzAMmsetbzOesJEGhNjkahq4n4WZTRU".

Start with k7_sk_ as a prefix and make it a 256bit key. Its stored on the device as a token to allow API communications.